import { ShieldCheck } from 'lucide-react'; import { useState } from 'react'; import { useNavigate } from 'react-router-dom'
export function LoginPage(){const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[loading,setLoading]=useState(false);const[error,setError]=useState('');const navigate=useNavigate();const submit=(e:React.FormEvent)=>{e.preventDefault();if(!email.includes('@')||password.length<6){setError('Enter a valid email and a password with at least 6 characters.');return}setError('');setLoading(true);window.setTimeout(()=>navigate('/dashboard'),550)};return <main className="login"><section><div className="brand"><ShieldCheck/>Sentinel Grid</div><p className="eyebrow">DEMO AUTHENTICATION</p><h1>Welcome to your command center.</h1><p>This is a UI-only demo flow. It does not authenticate users or securely store credentials.</p></section><form className="login-card" onSubmit={submit}><h2>Sign in</h2><p>Use any valid email and a 6+ character password to continue in demo mode.</p><label>Email<input value={email} onChange={e=>setEmail(e.target.value)} type="email" placeholder="you@example.com" autoComplete="email"/></label><label>Password<input value={password} onChange={e=>setPassword(e.target.value)} type="password" placeholder="••••••••" autoComplete="current-password"/></label>{error&&<p role="alert" className="form-error">{error}</p>}<button className="primary" disabled={loading}>{loading?'Opening workspace…':'Continue in demo mode'}</button><small>Production authentication, password hashing, and session handling are planned for Phase 2.</small></form></main>}
