import { Eye, EyeOff, ShieldCheck } from 'lucide-react'
import { useState } from 'react'
import { Link, useLocation, useNavigate } from 'react-router-dom'
import { ApiError } from '../services/apiClient'
import { useAuth } from '../auth/AuthContext'
export function LoginPage(){const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[showPassword,setShowPassword]=useState(false);const[loading,setLoading]=useState(false);const[error,setError]=useState('');const navigate=useNavigate();const location=useLocation();const{login}=useAuth();const success=location.state?.registered as boolean|undefined;const submit=async(e:React.FormEvent)=>{e.preventDefault();if(!email.includes('@')||!password){setError('Enter your email and password.');return}setLoading(true);setError('');try{await login({email,password});navigate((location.state?.from as string|undefined)??'/dashboard',{replace:true})}catch(reason){setError(reason instanceof ApiError&&reason.status===401?'Invalid email or password.':'Unable to sign in. Please try again.')}finally{setLoading(false)}};return <main className="login"><section><div className="brand"><ShieldCheck/>Sentinel Grid</div><p className="eyebrow">SECURE WORKSPACE</p><h1>Welcome to your command center.</h1><p>Sign in to access aggregate crowd-monitoring workspaces and safety operations.</p></section><form className="login-card" onSubmit={submit}><h2>Sign in</h2><p>Use your Sentinel Grid account to continue.</p>{success&&<p className="form-success">Account created. Please sign in.</p>}<label>Email<input value={email} onChange={e=>setEmail(e.target.value)} type="email" placeholder="you@example.com" autoComplete="email"/></label><label>Password<span className="password-field"><input value={password} onChange={e=>setPassword(e.target.value)} type={showPassword?'text':'password'} placeholder="••••••••" autoComplete="current-password"/><button type="button" aria-label="Show or hide password" onClick={()=>setShowPassword(!showPassword)}>{showPassword?<EyeOff size={17}/>:<Eye size={17}/>}</button></span></label>{error&&<p role="alert" className="form-error">{error}</p>}<button className="primary" disabled={loading}>{loading?'Signing in…':'Sign in'}</button><small>New to Sentinel Grid? <Link to="/register">Create an account</Link></small></form></main>}
