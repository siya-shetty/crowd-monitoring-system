const baseUrl=import.meta.env.VITE_API_BASE_URL??'http://localhost:8000'; export type HealthResponse={status:string;service:string;database?:string}; export async function getHealth():Promise<HealthResponse>{const response=await fetch(`${baseUrl}/health`);if(!response.ok)throw new Error(`Health check failed: ${response.status}`);return response.json() as Promise<HealthResponse>}
