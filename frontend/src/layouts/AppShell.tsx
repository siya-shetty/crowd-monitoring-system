import { Bell, ChevronDown, Menu, Moon, Search, ShieldCheck, Sun } from 'lucide-react'; import { useState } from 'react'; import { NavLink, Outlet } from 'react-router-dom'
const nav=[['/dashboard','Command Center'],['/live','Live Monitoring'],['/videos','Video Analysis'],['/analytics','Analytics'],['/events','Events'],['/alerts','Alerts'],['/incidents','Incidents'],['/cameras','Cameras'],['/settings','Settings']]
export function AppShell(){const[open,setOpen]=useState(false);const[dark,setDark]=useState(true);return <div className={dark?'app dark':'app'}><aside className={open?'sidebar open':'sidebar'}><div className="brand"><ShieldCheck/><span>Sentinel Grid</span></div><nav>{nav.map(([to,label])=><NavLink key={to} to={to} onClick={()=>setOpen(false)} className={({isActive})=>isActive?'active':''}>{label}</NavLink>)}</nav><div className="sidebar-foot">PHASE 1 · DEMO MODE</div></aside><main><header><button className="icon mobile" aria-label="Open navigation" onClick={()=>setOpen(!open)}><Menu/></button><div className="search"><Search size={17}/>Search workspace</div><div className="header-actions"><button className="icon" aria-label="Toggle theme" onClick={()=>setDark(!dark)}>{dark?<Sun/>:<Moon/>}</button><button className="icon badge" aria-label="Notifications"><Bell/></button><button className="profile">SS<ChevronDown size={15}/></button></div></header><section className="content"><Outlet/></section></main></div>}
