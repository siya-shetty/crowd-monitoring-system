import { Bell, ChevronDown, LogOut, Menu, Moon, Search, ShieldCheck, Sun } from 'lucide-react'; import { useState } from 'react'; import { NavLink, Outlet, useNavigate } from 'react-router-dom'; import { useAuth } from '../auth/AuthContext'
const nav=[['/dashboard','Command Center'],['/live','Live Monitoring'],['/videos','Video Analysis'],['/analytics','Analytics'],['/events','Events'],['/alerts','Alerts'],['/incidents','Incidents'],['/cameras','Cameras'],['/settings','Settings']]
export function AppShell(){const[open,setOpen]=useState(false);const[dark,setDark]=useState(true);const{user,logout}=useAuth();const navigate=useNavigate();const signOut=()=>{logout();navigate('/login',{replace:true})};return <div className={dark?'app dark':'app'}><aside className={open?'sidebar open':'sidebar'}><div className="brand"><ShieldCheck/><span>Sentinel Grid</span></div><nav>{nav.map(([to,label])=><NavLink key={to} to={to} onClick={()=>setOpen(false)} className={({isActive})=>isActive?'active':''}>{label}</NavLink>)}</nav><div className="sidebar-foot">AGGREGATE MONITORING</div></aside><main><header><button className="icon mobile" aria-label="Open navigation" onClick={()=>setOpen(!open)}><Menu/></button><div className="search"><Search size={17}/>Search workspace</div><div className="header-actions"><button className="icon" aria-label="Toggle theme" onClick={()=>setDark(!dark)}>{dark?<Sun/>:<Moon/>}</button><button className="icon badge" aria-label="Notifications"><Bell/></button><div className="user-summary"><span className="user-initial">{user?.full_name.slice(0,1).toUpperCase()}</span><span><strong>{user?.full_name}</strong><small>{user?.role}</small></span></div><button className="icon" aria-label="Log out" title="Log out" onClick={signOut}><LogOut/></button><button className="profile" aria-label="User menu"><ChevronDown size={15}/></button></div></header><section className="content"><Outlet/></section></main></div>}
